<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WasteBuddy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        /* Custom Futuristic Styling */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1a2a3a, #0d1b2a);
            color: #fff;
        }

        /* Navbar */
        .navbar {
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .navbar-brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: #00ff88 !important;
        }
        .navbar-brand img {
            filter: drop-shadow(0 0 5px #00ff88);
            width: 60px;
            height: 55px;
            border-radius: 50%;
            transition: transform 0.3s ease;
            margin-right: 10px;
        }
        .navbar-brand img:hover {
            transform: scale(1.1);
        }
        .nav-link {
            color: #fff !important;
            transition: all 0.3s ease;
        }
        .nav-link:hover {
            color: #00ff88 !important;
            transform: translateY(-3px);
        }
        .btn-light, .btn-warning {
            transition: all 0.3s ease;
        }
        .btn-light:hover, .btn-warning:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 10px #00ff88;
        }
        .btn-danger {
            transition: all 0.3s ease;
        }
        .btn-danger:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 10px #ff0000;
        }

        /* Responsive Navbar */
        @media (max-width: 992px) {
            .navbar-collapse {
                background: rgba(0, 0, 0, 0.7);
                backdrop-filter: blur(10px);
                padding: 10px;
                border-radius: 10px;
            }
        }
    </style>
</head>
<body>
    <!-- <select id="languageSelector">
        <option value="en">English</option>
        <option value="ml">Malyalam</option>
        <option value="hi">Hindi</option>
        <option value="ta">Tamil</option>
    </select> -->
    
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/index">
                <img class="logo" src="/images/logo.jpeg" alt="Logo">
                <span>WasteBuddy</span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <% if (user) { %>
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="/waste-classification"><i class="fas fa-camera me-1"></i> Classify</a></li>
                        <li class="nav-item"><a class="nav-link" href="/chatbot"><i class="fas fa-comments me-1"></i> Chatbot</a></li>
                        <li class="nav-item"><a class="nav-link" href="/carbon"><i class="fas fa-star me-1"></i> Carbon Footprint</a></li>
                        <li class="nav-item"><a class="nav-link" href="/pickup-scheduling"><i class="fas fa-truck me-1"></i> Schedule</a></li>
                        <li class="nav-item"><a class="nav-link" href="/marketplace"><i class="fas fa-store me-1"></i> Market</a></li>
                        <li class="nav-item"><a class="nav-link" href="/waste-data"><i class="fas fa-chart-bar me-1"></i> Data</a></li>
                        <li class="nav-item"><a class="nav-link" href="/flashcard"><i class="fas fa-book me-1"></i> Guide</a></li>
                    </ul>
                    <span class="text-light me-3">&nbsp;&nbsp;&nbsp; Hi, <%= user.name %></span>
                    <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
                <% } else { %>
                    <a href="/login" class="btn btn-light">Login</a>
                    <a href="/signup" class="btn btn-warning ms-2">Sign Up</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
